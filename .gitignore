# Local application repositories (managed by sync script)
local-app-repos/

# Secret files (never commit these!)
*-repo.yaml
*secret*.yaml

# Other common ignores
*.log
.DS_Store
Thumbs.db
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl
