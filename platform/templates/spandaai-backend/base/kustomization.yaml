apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: spandaai-backend-base

resources:
- deployment.yaml
- service.yaml
- ingress.yaml
- sealed-secret.yaml

commonLabels:
  app.kubernetes.io/name: spandaai-backend
  app.kubernetes.io/part-of: spandaai-platform
  app.kubernetes.io/version: "1.0.0"

images:
- name: your-registry/spanda-backend
  newTag: latest
